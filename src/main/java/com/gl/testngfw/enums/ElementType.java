package com.gl.testngfw.enums;

/**
 * Element types for creating objects id.
 */
public enum ElementType {
    BY_ID, BY_NAME, BY_XPATH, BY_CLASS, BY_TAG_NAME, BY_LINK_TEXT, BY_CSS_SELECTOR, BY_PARTIAL_LINK_TEXT;

}
